{
  "name": "@near-intents/axelar-data-provider",
  "version": "1.0.0",
  "description": "Axelar Network cross-chain data provider plugin for NEAR Intents",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@effect/language-service": "^0.41.1",
    "@module-federation/node": "^2.7.17",
    "@rspack/cli": "^1.5.2",
    "@rspack/core": "^1.5.2",
    "@types/node": "^24.6.0",
    "dotenv": "^17.2.2",
    "every-plugin": "catalog:",
    "msw": "^2.3.0",
    "sirv": "^3.0.2",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "build": "rspack build && tsc -p tsconfig.build.json",
    "type-check": "tsc -p tsconfig.build.json --noEmit",
    "dev": "rspack serve",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest --config vitest.integration.config.ts",
    "coverage": "vitest run --coverage"
  }
}
